java/net/minecraft/src/CodecMus.java
5 + import java.lang.reflect.*;
6 + import java.util.*;
10 - @Override
3 - import java.io.IOException;
13 - catch (IOException e) {
13 + catch (Exception e) {
5 + import java.net.*;
12 + Field f = this.getClass().getDeclaredField("url");
13 + f.setAccessible(true);
14 + URL url = (URL) f.get(this);
15 + f = this.getClass().getDeclaredField("urlConnection");
16 + f.setAccessible(true);
17 + URLConnection urlConnection = (URLConnection) f.get(this);
18 - return new MusInputStream(this, this.url, this.urlConnection.getInputStream());
18 + return new MusInputStream(this, url, urlConnection.getInputStream());
6 - import java.util.*;
