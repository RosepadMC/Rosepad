java/org/json/JSONObject.java
230 - final E val = (E)this.<Enum>optEnum((java.lang.Class<Enum>)clazz, key);
230 + final E val = this.optEnum(clazz, key);
1183 - final Collection<?> coll = object;
1183 + final Collection<?> coll = (Collection<?>) object;
1190 - final Map<?, ?> map = object;
1190 + final Map<?, ?> map = (Map<?, ?>) object;
1179 - if (object instanceof JSONObject || object instanceof JSONArray || JSONObject.NULL.equals(object) || object instanceof JSONString || object instanceof Byte || object instanceof Character || object instanceof Short || object instanceof Integer || object instanceof Long || object instanceof Boolean || object instanceof Float || object instanceof Double || object instanceof String || object instanceof BigInteger || object instanceof BigDecimal || object instanceof Enum) {
1179 + if (object instanceof JSONObject || object instanceof JSONArray || object instanceof JSONString || object instanceof Byte || object instanceof Character || object instanceof Short || object instanceof Integer || object instanceof Long || object instanceof Boolean || object instanceof Float || object instanceof Double || object instanceof String || object instanceof BigInteger || object instanceof BigDecimal || object instanceof Enum) {
1251 - final Map<?, ?> map = value;
1251 + final Map<?, ?> map = (Map<?, ?>) value;
1255 - final Collection<?> coll = value;
1255 + final Collection<?> coll = (Collection<?>) value;
